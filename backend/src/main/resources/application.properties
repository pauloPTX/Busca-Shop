# Supabase Database Configuration
spring.datasource.url=jdbc:postgresql://db.cswkswbrraulspybnofw.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=Paulo.dev12
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
# create = apaga e recria tabelas (use apenas na primeira vez)
# update = atualiza estrutura sem apagar dados (use depois)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=false

# Server Configuration
server.port=8080
server.servlet.context-path=/api

# Aumentar limite de upload para imagens Base64
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
server.tomcat.max-http-form-post-size=10MB

# CORS Configuration
spring.web.cors.allowed-origins=http://localhost:5173
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true
